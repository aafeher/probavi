{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://probavi.dev/schemas/adapter/teardown-response.json",
  "title": "Probavi adapter protocol v0 — teardown response payload",
  "description": "Confirmation that everything the adapter created outside the sandbox has been released (adapter-protocol.md §6.4).",
  "$comment": "Derived from docs/adapter-protocol.md, which is normative. On any disagreement the markdown document wins.",
  "type": "object",
  "properties": {
    "released": { "type": "boolean" }
  },
  "required": ["released"],
  "additionalProperties": false
}
